{
  "name": "homebridge-switchbot-curtain",
  "version": "0.0.1",
  "description": "homebridge plugin for SwitchBot Curtain.",
  "main": "index.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "clean": "rimraf ./dist",
    "lint": "eslint src/**.ts",
    "postpublish": "npm run clean",
    "prepublishOnly": "npm run lint && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm run build && npm link && nodemon"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/freedert/homebridge-switchbot-curtain.git"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "author": "iwk",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/freedert/homebridge-switchbot-curtain/issues"
  },
  "homepage": "https://github.com/freedert/homebridge-switchbot-curtain#readme",
  "devDependencies": {
    "@types/node": "^11.15.39",
    "eslint": "^7.14.0",
    "homebridge": "^1.1.6",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.2"
  },
  "dependencies": {}
}
